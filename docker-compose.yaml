services:
  api:
    extends:
      file: docker-compose.base.yaml
      service: api
    environment:
      - DEBUG=false
    volumes:
      - "./src/prodik/infrastructure/db/migrations/versions/:/prodik/src/prodik/infrastructure/db/migrations/versions/"

  prometheus:
    extends:
      file: docker-compose.base.yaml
      service: prometheus

  nginx:
    extends:
      file: docker-compose.base.yaml
      service: nginx

  node-exporter:
    extends:
      file: docker-compose.base.yaml
      service: node-exporter

  grafana:
    extends:
      file: docker-compose.base.yaml
      service: grafana

  postgres:
    extends:
      file: docker-compose.base.yaml
      service: postgres
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres", "-d", "postgres"]
      interval: 15s
      timeout: 15s
      retries: 5

volumes:
  prometheus-data:
  grafana-data:
  postgres-data:

networks:
  prodik-network:
    driver: bridge